---
applyTo: ".github/specs/**/*.md"
---

# Specs writing rules

- Write specs in the language specified by `DocLanguage` in `.github/copilot-instructions.md`
  (default English until `/setupSpecs` sets it).
- Keep acceptance criteria explicit and testable.
- Keep a `**Status:** ...` line near the top.
